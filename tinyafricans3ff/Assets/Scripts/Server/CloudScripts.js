// Cloud Script (JavaScript)
//handlers.helloWorld = function (args, context) {
/*
    var message = "Hello " + currentPlayerId + "!";
    log.info(message);
    var inputValue = null;
    if (args && args.hasOwnProperty("inputValue"))
        inputValue = args.inputValue;
    log.debug("helloWorld:", { input: inputValue });
    return { messageValue: message };
*/
//};

// This function will delete the specified character from the list allowed 
// by the user, and will also delete any inventory or VC currently held by 
// that character.
//handlers.deleteCharacterFromUser = function (args, context) {
/*
        var request = {
        PlayFabId: currentPlayerId,
        CharacterId: args.CharacterId,
        SaveCharacterInventory: args.SaveCharacterInventory
    };
    // The pre-defined "server" object has functions corresponding to each PlayFab server API 
    // (https://api.playfab.com/Documentation/Server). It is automatically 
    // authenticated as your title and handles all communication with 
    // the PlayFab API, so you don't have to write extra code to issue HTTP requests. 
    var DeleteCharacterResult = server.DeleteCharacterFromUser(request);
*/
//};
